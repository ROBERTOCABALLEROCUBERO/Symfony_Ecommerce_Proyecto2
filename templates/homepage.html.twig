{% extends 'base.html.twig' %}

{% block title %}Productos index
{% endblock %}

{% block body %}
	{% for productsChunk in ofertaProductosChunked %}
		<div class="row">
			{% for producto in productsChunk %}
				<div class="col-sm-3">
					<div class="card mb-4">
						<img src="#" alt="{{ producto.nombreProd }}" class="card-img-top">
						<div class="card-body">
							<h5 class="card-title">{{ producto.nombreProd }}</h5>
							<p class="card-text">{{ producto.descuento  }}
								%</p>
							<div class="d-flex justify-content-between align-item-center">
								<a href="{{ path('app_productos_show', {'id': producto.id}) }}" class="btn btn-dark">View details</a>
								<span class="h5 mt-auto">{{ producto.precio }}
									€</span>
							</div>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	{% endfor %}

{% endblock %}
