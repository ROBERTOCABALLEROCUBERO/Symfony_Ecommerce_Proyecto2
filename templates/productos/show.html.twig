{% extends 'base.html.twig' %}

{% block title %}Productos
{% endblock %}

{% block body %}
	{#     <h1>Productos</h1>
	
	    <table class="table">
	        <tbody>
	            <tr>
	                <th>Id</th>
	                <td>{{ producto.id }}</td>
	            </tr>
	            <tr>
	                <th>Nombre_prod</th>
	                <td>{{ producto.nombreProd }}</td>
	            </tr>
	            <tr>
	                <th>Cantidad</th>
	                <td>{{ producto.cantidad }}</td>
	            </tr>
	            <tr>
	                <th>Genero</th>
	                <td>{{ producto.genero }}</td>
	            </tr>
	            <tr>
	                <th>Precio</th>
	                <td>{{ producto.precio }}</td>
	            </tr>
	            <tr>
	                <th>Descuento</th>
	                <td>{{ producto.descuento }}</td>
	            </tr>
	            <tr>
	                <th>Talla</th>
	                <td>{{ producto.talla }}</td>
	            </tr>
	            <tr>
	                <th>Tipo</th>
	                <td>{{ producto.tipo }}</td>
	            </tr>
	            <tr>
	                <th>Fotoprod</th>
	                <td>{{ producto.fotoprod }}</td>
	            </tr>
	        </tbody>
	    </table> #}

	<div class="container">
		<div class="row mt-3 mb-4">
			<div class="col-md-4">
				<img src="https://via.placeholder.com/600x400" alt="{{ producto.nombreProd }}" class="img-fluid">
			</div>
			<div class="col-md-8">
				<h1 class="mt-4 mt-md-0">{{producto.nombreProd}}</h1>
				<h2>{{ producto.precio }}
					€</h2>
				<hr>
				<b>Descuento:
				</b>
				{{ producto.descuento }}
				{{ form_label(form.cantidad) }}
				{{ form_widget(form.cantidad, {
            'attr': {
                'class': 'form-control ' ~ (form.cantidad.vars.valid ? '' : 'is-invalid')
            }
        }) }}
				<div class="invalid-feedback">
					{{ form_errors(form.cantidad) }}
				</div>
			</div>
			{{ form_widget(form.add, {'attr': {'class': 'btn btn-warning w-25'}}) }}
			{{ form_end(form) }}
		</div>

		<a href="{{ path('app_productos_index') }}">back to list</a>

		<a href="{{ path('app_productos_edit', {'id': producto.id}) }}">edit</a>

		{{ include('productos/_delete_form.html.twig') }}
	{% endblock %}
