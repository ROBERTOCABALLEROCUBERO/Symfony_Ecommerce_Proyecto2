<form method="post" action="{{ path('app_productos_delete', {'id': producto.id}) }}" onsubmit="return confirm('¿Esta seguro de querer borrarlo?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto.id) }}">
    <button class="btn"><i class="fas fa-trash-alt"></i></button>
</form>
